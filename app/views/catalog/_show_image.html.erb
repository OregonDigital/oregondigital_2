<% if document.derivative_paths[:pyramidal] %>
  <%= openseadragon_picture_tag "/image-service/#{document.id}/info.json" %>
<% end %>
<%= render :partial => "show_default", :locals => {:document => document} %>
